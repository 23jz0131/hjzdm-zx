services:
  - type: web
    name: hjzdm-fullstack
    env: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: SERVER_PORT
        value: 9090
      - key: NODE_ENV
        value: production